//this was made so if the default font were changed, it would be easily changed in one location.
const defaults = {
        font: "Open Sans",
        textColor: "White"
    }

export default defaults;